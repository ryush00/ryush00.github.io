language: node_js
node_js:
- 6.9.4
branches:
  only:
  - source

install: npm install

before_script:
- git config --global user.name "ryush00"
- git config --global user.email "ryush00@gmail.com"
- sed -i "s/__GITHUB_TOKEN__/${__GITHUB_TOKEN__}/" _config.yml

script: npm run build
